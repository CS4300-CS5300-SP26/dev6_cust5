name: Deploy Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: bearestate-production-deploy
  cancel-in-progress: false

jobs:
  test:
    name: Run Django tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f myproject/requirements.txt ]; then pip install -r myproject/requirements.txt; fi
          pip install coverage

      - name: Run Django tests
        env:
          RENTCAST_API_KEY: dummy-test-key
          DEBUG: "False"
        run: |
          cd myproject
          coverage run --source='.' manage.py test
          coverage report -m --fail-under=80

  deploy:
    name: Deploy to BearEstateServer
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test

    steps:
      - name: Set up SSH key
        env:
          PROD_HOST: ${{ secrets.PROD_HOST }}
          PROD_SSH_PRIVATE_KEY: ${{ secrets.PROD_SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$PROD_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$PROD_HOST" >> ~/.ssh/known_hosts

      - name: Run deploy script on server
        env:
          PROD_HOST: ${{ secrets.PROD_HOST }}
          PROD_USER: ${{ secrets.PROD_USER }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          ssh -i ~/.ssh/id_ed25519 \
            -o BatchMode=yes \
            "${PROD_USER}@${PROD_HOST}" \
            "/usr/local/bin/bearestate-deploy.sh ${TARGET_SHA}"
